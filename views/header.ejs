<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8" />
		<title>Blog</title>
		<link rel="stylesheet" href="/stylesheets/style.css">
	</head>
	<body>

		<header>
			<h1><%= title %></h1>
		</header>

		<nav>
			<span><a title="home" href="/">Home</a></span>
			<% if (user) { %>
				<span><a title="post" href="/post">post</a></span>
				<span><a title="logout" class="logout" href="/logout">logout</a></span>
			<% } else { %>
				<span><a title="login" href="/login">login</a></span>
				<span><a title="register" href="/register">register</a></span>
			<% } %>
		</nav>
		<% if (success) { %>
			<div><%= success %></div>
		<% } %>
		<% if (error) { %>
			<div><%= error %> </div>
		<% } %>
		<article>
